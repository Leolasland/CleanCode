private static final Set<String> CHECK_NUMBERS = Set.of("123", "456", "234")
// номера проверок для документа, связывание во время компиляции.
Используется константа, для того чтобы если нужно изменить номера проверок, пришлось поменять это только один раз в коде.

String systemComment = getMessages("invoice_set.system_comment");
// комментарий документов, считывается из файла propertyes. Связывание во время выполнения программы.
Для изменения придется просто изменить содержимое файла.

elvenArchers.attack = 4;
// раннее связывание, во время написания кода. Используется так, для учебного примера, т.к. дает простой код.
В реальном рабочем проекте такое нельзя использовать, т.к. выглядит как магическое число (не понятно почему 4) и 
для изменения нужно менять везде, а где-то можно и не заметить.
